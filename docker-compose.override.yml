services:
  api:
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
    volumes:
      - ./app:/code/app
      - ./frontend/dist:/code/frontend/dist
    environment:
      - PYTHONPATH=/code

  # Для dev можно не поднимать monitoring